Create a modern animated landing page using Next.js (App Router) with the following requirements:

Tech stack:

* TypeScript
* Tailwind CSS
* Shadcn UI
* Framer Motion for UI animations
* GSAP for advanced animations (scroll-based, timeline)
* Lenis for smooth scrolling

Requirements:

* No backend, no API, no database
* Focus on high-quality animations and interactions
* Use App Router with server components where possible

Animations:

* Smooth scroll experience
* Scroll-triggered animations (fade, slide, parallax)
* Complex GSAP timelines
* Hover interactions and micro-animations
* Section transitions
* Mouse movement effects

Structure:

* /app
* /components
* /sections (Hero, About, Features, Gallery, CTA)
* /animations (custom animation logic)
* /hooks
* /assets

UI/UX:

* Modern, clean, premium design
* Fully responsive (mobile-first)
* Dark mode support
* Use reusable components

Performance:

* Lazy load heavy sections
* Dynamic import animation libraries
* Optimize images using next/image
* Avoid unnecessary re-renders

Extras:

* Add a hero section with strong visual impact
* Include parallax gallery
* Add animated text (typing / reveal)
* Add CTA with attention-grabbing animation

Generate full code with clean architecture and best practices.
